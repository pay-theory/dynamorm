# Python Contract Runner (minimal)

This runner validates shared, cross-language contract fixtures for the DynamORM Python implementation.

## Run

From the repo root:

```bash
uv --directory py run pytest -q ../contract-tests/runners/py
```

